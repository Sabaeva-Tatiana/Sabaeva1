#Задача 0 (Арифметические операции)
#Напиши код, который вычисляет сумму, разность, произведение и частное двух переменных a = 10 и b = 2.
# Результат каждого вычисления должен быть выведен в консоль в формате:
#Сумма: X, Разность: Y, Произведение: Z, Частное: W.

a = 10
b = 2
result = a + b
text = f"Сумма {result}"
print(text)

result = a - b
print(result)

result = a * b
print(result)

result = a / b
print(result)

#Задача 1 (Арифметические операции)
#Напиши код, который объявляет две переменные: a = 5 и b = 3.
# Затем выведи результат их сложения, вычитания, умножения и деления (каждое действие на новой строке).

a = 5
b = 3
result = a + b
print(result)

result = a - b
print(result)

result = a * b
print(result)

result = a / b
print(result)

#Объяви переменную num_str = "10" (строка) и переменную num_int = 5 (целое число).
#Преобразуй num_str в число, сложи его с num_int и выведи результат.
num_str = "10"
num_int = 5

num_int = int (num_str)
print(num_int)
num_int = 5
converted_num = int(num_str)
result = num_int + converted_num
print(result)

#Напиши код, который выводит результаты следующих сравнений (каждое с новой строки):
#x больше y?
#x меньше или равно y?
#Равны ли x и y?
#Формат вывода: True или False

x = 7
y = 10
print(x > y )
print( x <= y)
print( x == y)

#Задача 4 (Именование переменных)
#Перед тобой некорректно названные переменные:
#1num = 10
#my-var = "text"
#print = 50
#Исправь их имена, соблюдая правила Python, и выведи их значения.
#Подсказка: В Python имена переменных не могут начинаться с цифры, содержать дефисы или совпадать с ключевыми словами.

num = 10
my_var = "text"
number = 50

#Задача 5 (Переопределение переменной)
#Дана переменная:
#value = 100
#Выведи её текущее значение.
#Затем переопредели её, увеличив на 50, и выведи новое значение.

value = 100
print("Текущее значение:" , value)

value = value + 50
print("Новое значение:",value)

#Задача 6 (Классификация типов данных)
#Даны три переменные:
#var1 = 3.14
#var2 = "Hello"
#var3 = True
#Напиши код, который выводит тип данных каждой переменной в формате:
#Тип var1: <class 'float'> (и так для каждой).

var1 = 3.14
var2 = "Hello"
var3 = True

print("Тип var1" , type(var1))
print("Тип var2" ,type(var2))
print ("Тип var3" ,type(var3))

#Задача 7 (Преобразование типов данных)
#Даны переменные:
number = 50      # int
price = "9.99"   # str
flag = "True"    # str
#Сделай следующее:
#Преобразуй price в float, выведи тип и значение.
#Преобразуй flag в bool, выведи тип и значение.
#Формат вывода:
#text
#Тип price после преобразования: <class 'float'>. Значение: 9.99
#Тип flag после преобразования: <class 'bool'>. Значение: True

price = float(price)  # Преобразование строки в дробное число
print("Тип price после преобразования:", type(price), "Значение:", price)
flag = bool(flag)    # Преобразование строки в булево значение
print("Тип flag после преобразования:", type(flag), "Значение:", flag)

#Задача 8 (Объявление переменных)
#Объяви переменную name и запиши в неё своё имя (например, "Анна").
#Объяви переменную age и запиши в неё любой возраст (например, 20).
#Выведи обе переменные в одном print() через запятую.

name = "Татьяна"
age = 27
print ( name + " , " + str (age) )

#Задача 9 (Ещё проще! Базовый вывод)
#Напиши код, который выводит фразу:
#Меня зовут [ваше имя]. Мне [ваш возраст] лет.
#Подсказка: Используй print() и переменные из прошлой задачи.

name = "Татьяна"
age = 27
print(f"Меня зовут {name}. Мне {age} лет.")

#Задача 10 (Самый простой вариант)
#Напиши одну строку кода, которая выводит:
#Привет! Я изучаю Python.
#(Это задание на закрепление синтаксиса print()).

print( "Привет! Я изучаю  Python." )

#Задача 11 (Мини-практика)
#Объяви две переменные:
#fruit — запиши название фрукта (например, "яблоко"),
#count — запиши количество (например, 5).
#Выведи одной строкой:
#У меня есть 5 яблок.
#Подсказка: Используй f-строку или конкатенацию.

fruit = "банан"  # Объяви переменную
count = 7        # И эту
print(f"У меня есть {count} {fruit}а")  # Подставь переменные в строку

#Задача 12 (Базовые переменные)
#Объяви переменную city и запиши в неё название любого города.
#Объяви переменную population и запиши в неё число (например, население этого города).
#Выведи одной строкой:
#В [город] живёт [число] человек.
#Пример вывода:
#В Москве живёт 12000000 человек.

city = "Саратов"
population = 1000
print(f"В {city}e живёт {population} человек ")

#Задача 13 (Финал)
#Напиши код, который выводит:
#Python — это легко!
#Без использования переменных (просто один print).

print("Python - это легко!")

#(Арифметические операции)
#Напиши код, который объявляет две переменные: a = 5 и b = 3, затем выводит результат их сложения.
#Примечание: Следи за синтаксисом (например, нет ли лишних/пропущенных символов).

a = 5
b = 3
result = a + b
print(result)

#(Преобразование типов данных)
#Объяви переменную num_str = "10", преобразуй её в целое число и выведи тип данных результата с помощью type().
#(Проверь, чтобы преобразование было явным и вывод показывал тип <class 'int'>.)

#(Операции сравнения)
#Объяви две переменные: x = 7 и y = 10. Напиши код, который выводит True, если x меньше y, и False в противном случае.
#(Следи за правильным использованием оператора сравнения.)

x = 7
y = 10
result = x < y
print(result)

#(Именование переменных)
#Напиши код, где объявляешь корректно именованную переменную для хранения количества студентов (например, students_count),
# присваиваешь ей значение 50 и выводишь эту переменную.
students_count = 50
print(students_count)

#(Переопределение переменной)
#Объяви переменную price = 100, затем переопредели её значение на 90 и выведи новое значение.
#(Проверь, что выводится именно обновлённое значение.)

price = 100
price = 90  # переопределение
print(price)

#(Типы данных)
#Объяви переменную is_active = True, определи её тип данных с помощью type() и выведи результат.
#(Проверь, что вывод показывает <class 'bool'>.)

is_active = True
print(type(is_active))

#(Классификация по типам данных)
#Напиши код, который создаёт три переменные:
#name (строка),
#age (целое число),
#height (дробное число).
#Затем выведи их типы данных в одном print(), разделяя запятыми.
#(Пример вывода: <class 'str'>, <class 'int'>, <class 'float'>)

name = "Татьяна"
age = 27
height = 99.7
print(type(name), type(age), type(height))

#(Объявление переменных)
#Создай переменную book_title без присвоения значения (только объяви), а затем присвой ей строку "Python Basics" и выведи.
#(Проверь, чтобы код не вызывал ошибку UnboundLocalError.)

book_title = None # объявление без полезного значения
book_title = "Python Basics" # переопределение
print(book_title)

#(Арифметические операции)
#Объяви две переменные: x = 12 и y = 4. Напиши код, который выводит:
#Результат деления x на y (обычное деление),
#Результат целочисленного деления x на y.
#(Используй операторы / и //.)

x = 12
y = 4
result = x / y
print(result)
result = x // y
print(result)

#(Операции сравнения)
#Даны переменные:
#temperature = 25
#is_hot = temperature > 30
#Напиши код, который выводит "Требуется охлаждение", если is_hot равно True, и "Температура в норме" в противном случае.

temperature = 25
is_hot = temperature > 30
result = temperature > is_hot
print(result)

